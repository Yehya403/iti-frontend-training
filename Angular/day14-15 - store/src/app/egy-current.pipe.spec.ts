import { EgyCurrentPipe } from './egy-current.pipe';

describe('EgyCurrentPipe', () => {
  it('create an instance', () => {
    const pipe = new EgyCurrentPipe();
    expect(pipe).toBeTruthy();
  });
});
